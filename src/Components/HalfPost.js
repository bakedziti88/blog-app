/*
import React from 'react'

const HalfPost = ({post}) => {
	if (!post || Object.keys(post).length === 0) {
		return (
			<p>Select a post to view more about it</p>
		)
	}
		
	return (
		<div>
			<p>{post.url}</p>
				<Likes id = {post.id} likes = {post.likes} addLike = {like} />
			<br />
		</div>
	)
}

export default HalfPost
*/